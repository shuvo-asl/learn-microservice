meta {
  name: /lists
  type: http
  seq: 2
}

get {
  url: {{api-gateway-url}}/api/v1/accounts/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
