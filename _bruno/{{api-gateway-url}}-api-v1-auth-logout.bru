meta {
  name: {{api-gateway-url}}/api/v1/auth/logout
  type: http
  seq: 4
}

post {
  url: {{api-gateway-url}}/api/v1/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
